# Runtime volumes / local data — keep out of the build context
expose-data/
container-config/

# UI build outputs (rebuilt in Dockerfile anyway)
web/expose/dist/

# Node cache folders (rebuilt in Dockerfile)
web/expose/node_modules/
**/.pnpm-store/
**/.yarn/*
!**/.yarn/releases/**

# Go build cache / outputs
build/
bin/

# VCS & misc
.git/
.gitignore
.DS_Store
